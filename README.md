# olp.processing-service

```mermaid
gitGraph
  commit id: "Initial commit"
  branch develop
  checkout develop
  commit id: "Feature work"
  checkout main
  commit id: "Bug fix"
  merge develop tag: "Release v1.0"
